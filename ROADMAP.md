# ROADMAP

Межі фаз заморожені. Вихід за межі фази = ADR.

## Фаза 0 — Ядро без гри · **ЗАВЕРШЕНО 2026-09-14**
**Обсяг:** доменна модель бійця, генератор ~500 бійців, `engine-fight`, CLI для масових прогонів.
**Не входить:** UI, календар, гроші, рейтинги, ШІ-промоутери.
**Критерій завершення (гейт)** — звужено ADR-0009:
- 10 000 прогонів: M1, M3, M4 у коридорах ADR-0008
- M1(важка вага) > M1(легка вага) з розділеними довірчими інтервалами
- рушій детермінований: один seed → байт-у-байт однаковий EventLog
- покриття `engine-fight` тестами ≥ 80%
- `EventLog` містить `ending_round` і нокдауни, навіть попри те, що тестів на них поки немає

**Перенесено в гейт фази 1** (даних не існує — ADR-0009): M2 розподіл раунду зупинки,
M5 середня кількість нокдаунів на бій.

## Фаза 1 — Петля гри · **ЗАВЕРШЕНО 2026-09-16**
**Обсяг:** тік дня, стайбл гравця, табір і тренування, рейтинги одного органу, примітивний матчмейкінг ШІ, текстовий play-by-play, збереження/завантаження.
**Критерій:** можна зіграти 3 ігрові роки без збоїв; сейв відкривається після перезапуску.
**Виконано:** критерій перевірено тестом (три роки з перезапуском між кожним); обсяг закрито
повністю — тік дня, стайбл гравця, табір і тренування, рейтинги чотирьох органів, матчмейкінг ШІ,
текстовий play-by-play, збереження. Виміряно на закриття: 33 рішення гравця на рік,
жоден боєць не лишається без бою за два роки, темп 2.30 бою на рік.

> Уточнено 2026-09-16 (ADR-0024): «один стайбл гравця» означає **стайбл**, а не одного бійця.
> Кількість підопічних у рушії не обмежена; рекомендований старт — три.

## Фаза 2 — Світ · **розпочато 2026-09-16**
**Обсяг:** 4 санкційні органи, обов'язкові захисти, промоутери і переговори, економіка, вагова система, травми і накопичений знос, медіа й популярність, молодь і скаутинг.
**Критерій:** симуляція 10 ігрових років без участі гравця дає правдоподібну картину світу (чемпіони змінюються, кар'єри завершуються, з'являються нові зірки).

**Зроблено 2026-09-16 (ADR-0026, погоджено й реалізовано):** пояси всіх чотирьох органів,
титульні бої (12 раундів, вакансія розігрується між №1 і №2), обов'язкові захисти з власним
періодом на орган, позбавлення за пропущений захист. Виміряно на 5 роках/1500 бійцях: 67 з 68
поясів мають чемпіона, 8.2% боїв титульні, захистів у 9.5 раза більше за позбавлення.

**Зроблено 2026-09-16 (ADR-0027, погоджено й реалізовано):** фоли (low-blow, holding,
headbutt) і зняття балів — попередження на перше порушення кожного типу, бал знято з
другого; `dirtiness` вимірювано впливає на частоту. Гейт ADR-0008 перемірено з фолами
(`foulBaseChance` знижено з початкового здогаду 0.006 до виміряного 0.003) — коридори
M1/M3/M4 і допуск `tier-equivalence` (ADR-0015) лишилися незмінними.

**Зроблено 2026-09-16 (ADR-0028, погоджено й реалізовано):** покроковий прогін бою
`simulateFightSteps` із доведеним наскрізним потоком випадковості (`simulateFight` —
тонка обгортка над ним, біт-у-біт той самий лог); три стратегічні плани зі сценаріями
A/B/C замість «план A/B» з початкового ескізу ADR-0025; `coachSuitability` (придатність
плану бійцю, зашумлена скілом тренера — тимчасова шкала-заглушка); `proposeCornerAdvice`
(поради на раунд у межах уже обраного сценарію). Перевірено на демо-рівні: вибір
плану/сценарію, сітка придатності, застосовані поради кута — усе в headless Chromium.

## Фаза 3 — UI
**Обсяг:** повний набір екранів за ADR-0020, 2D-перегляд бою поверх EventLog, статистика, історія кар'єр.

**Погоджено 2026-09-16 (ADR-0025), лишається нереалізованим:**
- три режими перегляду бою — **результат**, **highlights**, **повна симуляція** — з темпом показу;
  усі три є поданням одного й того самого `EventLog` і не впливають на результат;
- повний екран бою з підтвердженням/автоприйняттям порад кута (механізм і дані вже
  готові — ADR-0028, фаза 2; лишається інтерфейс і інтеграція в тижневий цикл гравця
  замість/на додачу до `setFightPlan`, ADR-0023).

## Фаза 4 — Довге життя
**Обсяг:** редактор даних, модінг, мобільні збірки, онлайн-ліги.
